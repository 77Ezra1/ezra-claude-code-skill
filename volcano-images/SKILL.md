---
name: volcano-images
description: Volcano Engine (Jimeng/即梦) API image generation - Generate images using Chinese AI image generation service. Use when user mentions "配图", "生成图片", "illustration", "jimeng", "即梦", or needs images for articles. Supports New Yorker style illustrations, batch generation, and custom captions.
user-invocable: true
---


# 火山引擎配图 (Volcano Images)

使用火山引擎的即梦 API 为文章生成配图。

## 概述

**核心功能**：
- 调用即梦 API 生成高质量图片
- 支持《纽约客》风格配图
- 并发生成，提高效率
- 自动插入 Markdown 文章

**支持模型**：
- `jimeng-image-4.5`（付费，高质量）
- `jimeng-image-4.1`（免费，基础质量）

## 适用场景

- 用户说「配图」「生成图片」「为文章配图」
- 用户说「jimeng」「即梦」「火山引擎」
- book-interpreter 或 qiaomu-paper-interpreter 需要配图

## 执行流程

### 模式 A：为 Markdown 文章配图

**触发词**：「为这篇文章配图」「生成配图」

**执行步骤**：

1. **分析文章结构**：
   ```bash
   python ~/.claude/skills/volcano-images/scripts/analyze_article.py <markdown_file> --create-template
   ```
2. **填写配图配置**：Claude 分析每个 H2 标题，设计视觉描述
3. **批量生成配图**：
   ```bash
   python ~/.claude/skills/volcano-images/scripts/generate_images.py \
     <markdown_file> --workers 3
   ```

### 模式 B：单张图片生成

**触发词**：「生成一张配图」「画一张图」

**执行步骤**：

```bash
python ~/.claude/skills/volcano-images/scripts/generate_single.py \
  --prompt "视觉描述" \
  --caption "底部标题" \
  --output output.png
```

### 模式 C：批量生成（自定义配置）

**执行步骤**：

1. 创建 JSON 配置文件：
   ```json
   {
     "images": [
       {
         "prompt": "具体的视觉场景描述（中文）",
         "caption": "底部中文标题",
         "filename": "image_01.png"
       }
     ]
   }
   ```

2. 执行批量生成：
   ```bash
   python ~/.claude/skills/volcano-images/scripts/generate_batch.py \
     config.json --output ./images --workers 3
   ```

## 配置要求

### 视觉描述规范

详见 [references/visual-guide.md](references/visual-guide.md)，核心原则：

- ✅ **使用中文描述**（即梦 API 是国产模型）
- ✅ **描述具体场景**（物体、动作、空间关系）
- ✅ **用场景隐喻抽象概念**（由 Claude 完成）
- ❌ **不包含文字指令**（如"底部标题"、"标注"等）

### 错误示例

```
❌ "生成一张关于深度学习的图，底部要有标题"
❌ "画一个神经网络，标注神经元和连接"
```

### 正确示例

```
✅ "复杂的立体神经网络结构，无数发光的节点通过光线连接，像宇宙中的星系网络"
✅ "一只猫在读书，旁边堆满了各种书籍，阳光透过窗户洒在书页上"
```

## API 配置

### 环境变量

在 `.env` 文件或环境变量中配置：

```bash
# 即梦 API 配置
JIMENG_API_URL=http://localhost:8000/v1/images/generations
JIMENG_API_KEY=your_api_key_here
JIMENG_MODEL=jimeng-image-4.5  # 或 jimeng-image-4.1（免费）
```

### 本地 Docker 服务

使用本地 Docker 容器运行即梦 API：

```bash
docker run -d \
  -p 8000:8000 \
  -e JIMENG_SESSION_ID=your_session_id \
  ghcr.io/zhizinan1997/jimeng-free-api-all:latest
```

## 使用示例

### 示例 1：为文章自动配图

```
用户：为这篇解读文章配图

Claude：
📊 正在分析文章结构...
   找到 8 个 H2 标题

🎨 生成配图配置...
   填写每个章节的视觉描述

🚀 开始批量生成（3并发）...
   [1/8] ✅ 深度的悖论 (28秒)
   [2/8] ✅ 网络架构 (29秒)
   ...

✅ 配图完成！
📁 已保存到: articles/images/
📝 已插入到: article_解读.md
```

### 示例 2：单张配图

```
用户：生成一张关于"注意力机制"的配图

Claude：
正在生成配图...

✅ 生成完成！
📄 文件: attention_mechanism.png
🎨 风格: 纽约客钢笔画
⏱️ 耗时: 28秒
```

## 配图风格

### 《纽约客》风格（默认）

- 钢笔墨水速写
- 黑白线条 + 朱红色点缀
- 16:9 比例
- 简洁留白，松弛线条
- 底部中文标题

### 自定义风格

通过 `--style` 参数指定：

```bash
python ~/.claude/skills/volcano-images/scripts/generate_single.py \
  --prompt "..." \
  --style "水彩画" \
  --output output.png
```

## 文件结构

```
~/.claude/skills/volcano-images/
├── SKILL.md                    # 本文件
├── scripts/
│   ├── generate_images.py      # 批量生成配图
│   ├── generate_single.py      # 单张图片生成
│   ├── analyze_article.py      # 分析文章结构
│   └── image_api.py            # 即梦 API 封装
└── references/
    └── visual-guide.md         # 视觉描述指南
```

## 性能优化

- **并发数建议**：3-4 线程，避免 API 限流
- **单张耗时**：约 28-30 秒
- **批量生成**：3 并发下，12 张图约 2 分钟

## 故障排查

| 问题 | 解决方案 |
|------|----------|
| 连接失败 | 检查 Docker 容器是否运行 |
| 生成超时 | 检查网络连接，增加重试次数 |
| 图片质量差 | 尝试 jimeng-image-4.5 模型 |
| 描述不匹配 | 优化视觉描述，更具体 |

## 与其他 Skills 配合

1. **zlibrary** → 下载 EPUB 书籍
2. **epub-to-markdown** → 转换为 Markdown
3. **book-interpreter** → 按 Ezra 风格解读
4. **volcano-images** → 为文章配图 ⬅️ 当前
